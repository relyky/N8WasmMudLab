@using System.Reactive.Subjects

<div class="box">
  <h3>PlusProducer</h3>

  <button @onclick=HandleClick>＋１</button>

</div>

@code {
  [CascadingParameter] _RXDEMO02 BizState { get; set; } = default!;

  //# Property
  Subject<int> PlusSubject => BizState.plusSubject;

  void HandleClick()
  {
    //PlusSubject.OnNext(Random.Shared.Next(1,7));
    PlusSubject.OnNext(+1);
  }
}
